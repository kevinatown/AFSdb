<%= form_for(@company_category) do |f| %>
  <% if @company_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company_category.errors.count, "error") %> prohibited this company_category from being saved:</h2>

      <ul>
      <% @company_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
