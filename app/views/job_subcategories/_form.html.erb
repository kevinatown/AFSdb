<%= form_for(@job_subcategory) do |f| %>
  <% if @job_subcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_subcategory.errors.count, "error") %> prohibited this job_subcategory from being saved:</h2>

      <ul>
      <% @job_subcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
