<%= form_for(@streetdir) do |f| %>
  <% if @streetdir.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@streetdir.errors.count, "error") %> prohibited this streetdir from being saved:</h2>

      <ul>
      <% @streetdir.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
